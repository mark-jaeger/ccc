---
phase: 02-flow-migration
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tmux/sessions.go
  - tmux/sessions_test.go
  - CLAUDE.md
autonomous: true
requirements: [MIGR-05]

must_haves:
  truths:
    - "tmux package directory no longer exists"
    - "No imports reference the tmux package"
    - "Full test suite passes without tmux"
    - "Documentation updated to reflect abduco"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated architecture documentation"
      contains: "abduco"
  key_links:
    - from: "flow/common.go"
      to: "abduco/sessions.go"
      via: "import"
      pattern: "github.com/mark-jaeger/ccc/abduco"
---

<objective>
Delete tmux package and update documentation to complete the migration.

Purpose: Remove the now-unused tmux package to prevent confusion and update project documentation to reflect the new abduco-based architecture.

Output: Deleted tmux/ directory, updated CLAUDE.md with abduco references.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan that migrated flow package
@.planning/phases/02-flow-migration/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify no tmux imports remain</name>
  <files></files>
  <action>
Before deleting the tmux package, verify no code references it:

1. Search for tmux imports in all Go files:
   ```bash
   grep -r "mark-jaeger/ccc/tmux" --include="*.go" .
   ```

2. If any imports found, this task fails - Plan 01 must be completed first.

3. Verify the build works without tmux being used:
   ```bash
   go build ./...
   ```

This is a safety check before deletion.
  </action>
  <verify>
    <automated>! grep -r "mark-jaeger/ccc/tmux" --include="*.go" . && go build ./...</automated>
  </verify>
  <done>No Go files import the tmux package, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Delete tmux package</name>
  <files>tmux/sessions.go, tmux/sessions_test.go</files>
  <action>
Delete the entire tmux directory:

```bash
rm -rf tmux/
```

Then verify:
1. Directory no longer exists
2. Full test suite still passes
3. Build still succeeds

The tmux package contained:
- sessions.go (284 lines)
- sessions_test.go (test file)

Both are now obsolete since flow uses abduco package.
  </action>
  <verify>
    <automated>test ! -d tmux/ && go build ./... && go test ./...</automated>
  </verify>
  <done>tmux/ directory deleted, all builds and tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md documentation</name>
  <files>CLAUDE.md</files>
  <action>
Update CLAUDE.md to reflect the tmux-to-abduco migration:

**Section: Architecture (first paragraph)**
- Change: "ccc is a CLI tool for managing tmux sessions"
- To: "ccc is a CLI tool for managing abduco sessions"

**Section: Package Dependency Graph**
Change:
```
main -> flow -> config, ssh, tmux, scan, ui, tailscale
                 ssh -> config, internal/shellutil
                scan -> internal/shellutil
                tmux -> internal/shellutil
```
To:
```
main -> flow -> config, ssh, abduco, scan, ui, tailscale
                 ssh -> config, internal/shellutil
                scan -> internal/shellutil
              abduco -> internal/shellutil
```

**Section: Control Flow**
- Change: "tmux attach/create"
- To: "abduco attach/create"

**Section: Key Design Patterns - Command building**
- Change: "Packages (`tmux`, `scan`)"
- To: "Packages (`abduco`, `scan`)"

**Section: Key Design Patterns - Session metadata**
Change entire bullet:
- From: "Session metadata: tmux sessions are tagged with @ccc_project and @ccc_path user options. FilterSessionsForProject matches by metadata first (Verified=true), then by name prefix (Verified=false, shown with warning)."
- To: "Session naming: abduco sessions use `ccc.{project}.{suffix}` naming convention. FilterSessionsForProject matches by Project field, with External sessions (non-ccc prefix) shown with warning."

Keep all other sections unchanged (SSH Modes, Testing, etc).
  </action>
  <verify>
    <automated>grep -q "abduco" CLAUDE.md && ! grep -q "managing tmux sessions" CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md updated to document abduco-based architecture</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `test ! -d tmux/` - tmux directory deleted
2. `go build ./...` - All packages build
3. `go test ./...` - Full test suite passes
4. `grep -q "abduco" CLAUDE.md` - Documentation updated
5. `! grep -r "tmux" --include="*.go" .` - No tmux references in Go code
</verification>

<success_criteria>
1. tmux/ directory no longer exists
2. No Go files import or reference the tmux package
3. Full build and test suite passes
4. CLAUDE.md documents abduco instead of tmux
5. Package dependency graph updated
</success_criteria>

<output>
After completion, create `.planning/phases/02-flow-migration/02-02-SUMMARY.md`
</output>
